export const LocationNavigator = ({nombreBoton}) => {

    const currentLocation = 'A Dark Forest'
    const subLocation = 'Twisted Tree'

    return (
        <div>

            <h1>{currentLocation}</h1>

            <h2>{nombreBoton}</h2>
            
        </div>
    )
}
